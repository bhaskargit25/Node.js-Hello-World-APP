name: Deploy to ECS

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.bhaskar739 }}
          password: ${{ secrets.Bhaskar25@ }}

      - name: Build and push Docker image
        run: |
          docker build -t bhaskar739/hello-world:latest .
          docker push bhaskar739/hello-world:latest

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v1

      - name: Initialize Terraform
        run: terraform init
        working-directory: ./terraform

      - name: Apply Terraform
        run: terraform apply -auto-approve
        working-directory: ./terraform
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AKIAXYW3MCQATDJC7BFU }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.eH/ZLPkUh8YxJQknvbbzMKrJj/jgKO9bRjYGOAFd }}
